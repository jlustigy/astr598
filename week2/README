Description:
	This python program initializes an empty LinkedList, inserts 3 values at the head, deletes one value at the head, and inserts 3 values at the tail. It also prints at each step so the user can verify that it is working correctly. 

Files:
	linkedlist.py: 
		This file contains the Node and LinkedList classes. 
	myprogram.py: 
		Run this file to execute the behavior described in the description. 

Usage:
	$ qsub -I
	$ cd <to/this/dir>
	$ module load anaconda_2.4
	$ python myprogram.py

