Description:
    This python program initializes an empty stack, adds 10 random integers (between 0-100) to the stack, then one at a time pops each value from the stack and prints it for the user. 
    
Files:
    stack.py: 
        Contains the Node and Stack classes. 
    myprogram.py: 
        Run this python program to execute the behavior described in the description.

Usage:
    1. Log into hyak:
        $ ssh <username>@hyak.washington.edu
    2. Enter an interactive session:
        $ qsub -I
    3. Load anaconda 2.4
        $ module load anaconda_2.4
    4. Move to directory containing these files:
        $ cd to/this/directory
    5. Run python code:
        $ python myprogram.py

